table:
  schema: public
  name: slashing_params
select_permissions:
  - role: anonymous
    permission:
      columns:
        - one_row_id
        - params
        - height
      filter: {}
      allow_aggregations: true
