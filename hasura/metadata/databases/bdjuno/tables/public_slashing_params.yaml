table:
  name: slashing_params
  schema: public
select_permissions:
- permission:
    allow_aggregations: true
    columns:
    - one_row_id
    - params
    - height
    filter: {}
  role: anonymous
