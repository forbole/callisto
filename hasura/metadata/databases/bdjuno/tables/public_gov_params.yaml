table:
  schema: public
  name: gov_params
select_permissions:
  - role: anonymous
    permission:
      columns:
        - one_row_id
        - deposit_params
        - voting_params
        - tally_params
        - height
      filter: {}
      allow_aggregations: true
