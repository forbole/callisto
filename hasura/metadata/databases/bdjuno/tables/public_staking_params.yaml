select_permissions:
- permission:
    allow_aggregations: true
    columns:
    - one_row_id
    - bond_denom
    - unbonding_time
    - max_entries
    - historical_entries
    - max_validators
    - height
    filter: {}
  role: anonymous
table:
  name: staking_params
  schema: public
