table:
  name: wasm_params
  schema: public
select_permissions:
- permission:
    allow_aggregations: true
    columns:
    - one_row_id
    - code_upload_access
    - instantiate_default_permission
    - height
    filter: {}
  role: anonymous
