table:
  name: distribution_params
  schema: public
select_permissions:
  - role: anonymous
    permission:
      columns:
        - one_row_id
        - params
        - height
      filter: {}
      allow_aggregations: true
