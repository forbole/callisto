table:
  name: fee_grant_allowance
  schema: public
select_permissions:
  - permission:
      allow_aggregations: true
      columns:
        - id
        - grantee_address
        - granter_address
        - allowance
        - height
      filter: {}
    role: anonymous