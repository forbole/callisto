table:
  name: average_block_time_per_minute
  schema: public
select_permissions:
  - role: anonymous
    permission:
      columns:
        - average_time
        - height
      filter: {}
      allow_aggregations: true
