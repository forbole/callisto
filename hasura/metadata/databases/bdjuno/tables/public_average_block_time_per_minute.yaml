table:
  schema: public
  name: average_block_time_per_minute
select_permissions:
  - role: anonymous
    permission:
      columns:
        - average_time
        - height
      filter: {}
      allow_aggregations: true
