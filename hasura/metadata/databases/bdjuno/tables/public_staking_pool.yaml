table:
  schema: public
  name: staking_pool
select_permissions:
  - role: anonymous
    permission:
      columns:
        - height
        - bonded_tokens
        - not_bonded_tokens
      filter: {}
      allow_aggregations: true
