CREATE TABLE modules(
    auth boolean,
    bank boolean,
    consensus boolean,
    distribution boolean,
    mint boolean,
    pricefeed boolean,
    staking boolean,
    supply boolean,
    timestamp TIMESTAMP
);